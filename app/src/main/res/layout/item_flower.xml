<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/_10sp"
    android:background="@drawable/flower_bg"

    android:padding="@dimen/_10sp">

    <ImageView
        android:id="@+id/ivFlowerImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintWidth_percent="0.3"
        android:scaleType="fitCenter"
        tools:src="@drawable/app_icon" />

    <TextView
        android:id="@+id/tvFlowerName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceSubtitle1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/ivFlowerImage"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.7"
        android:paddingStart="8dp"
        android:textColor="@color/black"
        android:paddingEnd="8dp"
        tools:text="Flower Name" />

    <TextView
        android:id="@+id/tvFlowerDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:textAppearance="?attr/textAppearanceBody2"
        app:layout_constraintTop_toBottomOf="@id/tvFlowerName"
        app:layout_constraintStart_toEndOf="@id/ivFlowerImage"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.7"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        tools:text="Flower description goes here. This is a sample description to illustrate how it will look." />

</androidx.constraintlayout.widget.ConstraintLayout>
